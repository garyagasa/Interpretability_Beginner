# Optional — ARENA Chapter 2 (RL). NOT needed for lec1–lec12 notebooks.
# Requires Python 3.11+ (jax 0.10.x does not support 3.10).
#
# Install (separate env recommended):
#   conda create -n arena-rl python=3.11
#   conda activate arena-rl
#   pip install -r requirements-rl.txt

-r requirements.txt

pygame
gymnasium[atari, accept-rom-license, other, mujoco-py]==0.29.0
mujoco
jax[cuda12]==0.10.1
brax==0.14.2
envpool==1.2.5
